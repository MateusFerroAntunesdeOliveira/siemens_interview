#include <iostream>

using namespace std;

int main() {
    cout << "\nLevando em consideracao que o truncamento da tabela vai" << endl;
    cout << "apagar seus dados (e nao a tabela tab), a partir do momento que" << endl;
    cout << "for inserido os valores (3,3) e (4,4), estes serao mostrados na" << endl;
    cout << "selecao de tudo no comando (select count(*))" << endl << endl;

    return 0;
}
